/**
 * The direction of a {@link NodeAssetConnectionPoint}.
 */
export enum NodeAssetConnectionPointDirection {
    /** The connection point consumes a value. */
    Input,
    /** The connection point produces a value. */
    Output,
}
